apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-puzzle-solver-prod

resources:
  - ../../base

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: ai-puzzle-solver-backend
